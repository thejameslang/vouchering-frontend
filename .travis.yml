language: node_js

cache:
  directories:
    - "node_modules"

script:
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master

notifications:
    slack: cbrecode:Sj3O9DRJNMCBit4SGea5WK4I